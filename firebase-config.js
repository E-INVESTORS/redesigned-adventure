// Optional: paste your Firebase Web app config here to enable Firebase auth.
// When ready, uncomment and set window.FIREBASE_CONFIG to your config object.

/* Example:
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
*/

// Keep file empty (or set FIREBASE_CONFIG) if you want to use only localStorage auth.
